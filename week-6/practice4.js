
function handleSubmit() {
  // Create an (ordered) array of month names from January to December

  // Have the user enter a month number between 1 and 12.
  // - First verify that the user entered a number and that it is within the range 1 to 12.
  //   Produce an error message if they did not.
  // - If they entered a valid number, use an array method to
  //   to output the correct month name from the array you created above.
}
